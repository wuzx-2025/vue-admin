spring:
  application:
    name: backend-spring-ai
  datasource:
    url: jdbc:mysql://localhost:3306/bl_system?useSSL=false&serverTimezone=UTC&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password: 11111111
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # change to validate or none in production
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: false

jwt:
  secret: very-please-change-this-secret-in-prod
  expiration-ms: 3600000
